name: Readme
on:
  workflow_dispatch: {}
jobs:
  readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.README_TOKEN }}
      - uses: actions/setup-node@v2
      - run: |
          npm ci
          npm start
